// Establishes connection with the game server.
const IP = "10.0.2.15";
const PORT = 50541;

const moveUp = "Move: up";
const moveDown = "Move: down";
const moveLeft = "Move: left";
const moveRight = "Move: right";
const bamText = "Say: BAM";
const ftwText = "Say: For the Win!";

module.exports = {
  IP,
  PORT,
  moveUp,
  moveDown,
  moveLeft,
  moveRight,
  bamText,
  ftwText,
};
